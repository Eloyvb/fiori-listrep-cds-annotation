{
  "name": "listrep_cds_1",
  "version": "1.0.0",
  "description": "List Rep. with annotation cds",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8.4.0",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.6.5",
    "@cap-js/sqlite": "^1.7.5"
  },
  "cds":{
    "[dev]":{
      "requires": {
        "db": {
          "kind": "sqlite",
          "credentials": {
            "database": "db.sqlite"
          }
        }
      }
    }
  },
  "scripts": {
    "start": "cds-serve"
  }
}
